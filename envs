# Scripts configuration
#PLATFORM_IP="$( ipconfig getifaddr en0 )"
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
VERSION_ID=v2
VERSION_PATH=/push/${VERSION_ID}
EXECUTABLE=gorush
DEPLOY_ACCOUNT=jaraxasoftware
DEPLOY_IMAGE=${EXECUTABLE}${VERSION_ID}
CONTAINER=js-${EXECUTABLE}-${VERSION_ID}
IMAGE_FILE=${DEPLOY_ACCOUNT}_${EXECUTABLE}_${VERSION_ID}
PORT=10422
